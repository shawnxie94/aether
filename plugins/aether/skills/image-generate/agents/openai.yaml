interface:
  display_name: "Aether Image Generate"
  short_description: "Generate or edit images only on request"
  brand_color: "#4F7CAC"
  default_prompt: "Use $image-generate to generate or edit an image from an approved refined prompt and record the Aether run."
policy:
  allow_implicit_invocation: false
