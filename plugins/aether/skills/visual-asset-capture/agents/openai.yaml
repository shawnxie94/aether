interface:
  display_name: "Aether Visual Asset Capture"
  short_description: "Capture reusable visual assets from reference images"
  brand_color: "#4F7CAC"
  default_prompt: "Use $visual-asset-capture to analyze these reference image(s) and source prompt(s), then save reusable visual assets."
policy:
  allow_implicit_invocation: false
