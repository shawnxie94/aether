interface:
  display_name: "Aether Orchestrator"
  short_description: "Route Aether visual asset, prompt, and image tasks"
  brand_color: "#4F7CAC"
  default_prompt: "Use $aether-orchestrator to route this Aether request to the local panel, visual asset capture, prompt refinement, or explicit image generation."
policy:
  allow_implicit_invocation: true
